import React from 'react';

function Icon({name, color}) {
    // TODO - 1/ logic Name vs Path. 2/ add properties
    // color - size(S/M/L) - weight - alignement

    return (
        <i></i>
    );
}

export default Icon;