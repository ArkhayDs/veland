import React from 'react';

function Image({path}) {
    // TODO - 1/ logic Name vs Path. 2/ add properties
    // size(S/M/L/100%) - display mode

    return (
        <img src={path} alt={}></img>
    );
}

export default Image;