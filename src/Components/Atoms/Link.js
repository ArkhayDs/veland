import React from 'react';

function Link(props) {
    // TODO - add properties + disable default 'a' css
    // href - color - status

    return (
        <a href={props}></a>
    );
}

export default Link;